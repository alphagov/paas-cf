---
- type: replace
  path: /releases/-
  value:
    name: "prometheus"
    version: "27.2.2"
    url: "https://bosh.io/d/github.com/cloudfoundry-community/prometheus-boshrelease?v=27.2.2"
    sha1: "05ad1bff179a7e1eceb24115c5a129f44a2fc9f9"

- type: replace
  path: /addons?/-
  value:
    name: prometheus-blackbox-exporter
    exclude:
      deployments:
        - concourse  # from paas-bootstrap
        - prometheus # has a separate version
    jobs:
      - name: blackbox_exporter
        release: prometheus
        properties:
          blackbox_exporter:
            config:
              modules:
                dns_canary:
                  prober: dns
                  timeout: 1s
                  dns:
                    query_name: __canary.((system_domain))
                    query_type: "A"
