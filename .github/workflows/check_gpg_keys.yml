---
on:
  pull_request:
    paths:
      - ".gpg-id"

jobs:
  check_gpg_keys:
    runs-on: ubuntu-latest

    steps:
      ## Setup
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Check GPG keys
        run: |
          ./scripts/check_gpg_keys.sh